version: '1.0'

services:
  react-app-dev:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5190:5190"
    environment:
      - NODE_ENV=production # ya development, jo chahiye wo rakho
    volumes:
      - .:/app
    labels:
      - subdomain=te.workbrink.com
      - websiteName=travelease-frontend
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:5190" ]
      interval: 30s
      timeout: 10s
      retries: 3
